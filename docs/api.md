# amnesiac — публичный API v0.2

**Версия:** v0.2 · последнее изменение 2026-09-06: §5.1 — новые исключения в контракте вызова; §5.2 — пустой ответ повторяется (D-028, отменяет D-017), список транзиентных расширен (D-029); §5.7 — сложение `Usage` (D-031); §5.8–5.10 — раздельные стадии, `AxisSummariesResult`, `MetaResult` (D-030); §7 — порядок блоков меты; §8 — `MetaSummaryError` и `axis_summaries` в `TooManyAxisFailures` (D-032); §9 расширен до **восьми**; §10 — два имени выведены из непубличных. До этого 2026-08-23: §6 — тип входа не приводится (D-021) и валидация `top_k` (D-022); §9 — оговорка про границу потребителя, список **из шести**. 2026-08-22: §5.2, §5.3, §5.6 под D-017/D-018/D-019, §9 расширен до шести (D-020), §4 — уточнение про сортировку.

Документ — source of truth по публичному контракту пакета. По разделу 9.1 регламента публичный API меняется только через него: изменение сигнатуры, состава полей модели или наблюдаемого поведения мимо `api.md` — дефект спринта независимо от качества кода.

Версия контракта: **0.2**. Дата принятия: 2026-09-06. Спринт: E01.5/S01. Контракт v0.1 принят 2026-08-21 в E01/S01.

Изменения v0.2 строго аддитивны: сигнатура `summarize()` и её поведение на успешном пути не менялись, `SummarizeError` остаётся общим предком, существующий код потребителя работает без правок. Наблюдаемо изменилось одно — число обращений к провайдеру на неуспешных путях (§5.2).

---

## 1. Что пакет делает

Двухстадийная суммаризация новостного потока: тематические axis-саммари параллельно, затем сборка мета-нарратива над ними. Стадии доступны и одним вызовом, и по отдельности (§5.8–5.10) — вторая стадия собирает мету из готовых саммари, не обращаясь к документам. Плюс чистая математика отбора документов по осям.

Пакет не трогает БД, сервис эмбеддингов, конфиги, файловую систему и переменные окружения. Не создаёт LLM-клиента, не читает ключи, не пишет результат. Получает данные аргументами, возвращает данные. Всё перечисленное — обязанность потребителя.

Пакет исполняется в цикле событий вызывающего кода и своего не создаёт (D-011).

## 2. Уровни стабильности

**Стабильно на `0.x`** — имя, сигнатура, состав полей и наблюдаемое поведение не меняются внутри мажорной ветки `0.x`. Ломающее изменение требует поднятия минорной версии и записи в этом документе.

**Unstable** — может измениться в любом релизе, включая патч. Потребитель, опирающийся на такое имя, пиннит точную версию.

Имена, введённые в v0.2 — `summarize_axes`, `summarize_meta`, `AxisSummariesResult`, `MetaResult`, `MetaSummaryError`, `Usage.__add__` — **стабильны на `0.x`** наравне с остальным ядром. Публикация раздельных стадий является обещанием на всю мажорную ветку, а не экспериментом: потребитель строит на них восстановление после сбоя, то есть путь, который включается ровно тогда, когда что-то уже пошло не так.

Всё, что не описано в этом документе, публичным API не является — см. раздел 10.

---

## 3. Быстрый старт

```python
from amnesiac import Doc
from amnesiac.summarize import PromptPack, SummarizeConfig, summarize
from amnesiac.summarize.prompts import RU_MACRO_V1

result = await summarize(
    client=client,                       # AsyncOpenAI, чей — не важно
    model="deepseek/deepseek-v4-flash",
    axes={"дкп": [Doc(...), ...], "инфляция": [...]},
    prompts=RU_MACRO_V1.bind(horizon_days=14),
    limiter=limiter,                     # необязателен, см. 5.2
    config=SummarizeConfig(temperature=0.3, max_failed_axes=2),
)

result.meta            # str — мета-саммари
result.axis_summaries  # dict[str, str]
result.failed_axes     # list[str]
result.axis_errors     # dict[str, str]
result.usage           # Usage
```

Частичный пересчёт, когда часть осей уже посчитана и лежит у потребителя:

```python
from amnesiac.summarize import summarize_axes, summarize_meta

missing = {name: docs for name, docs in axes.items() if name not in cached}

fresh = await summarize_axes(client=client, model=model, axes=missing,
                             prompts=prompts, limiter=limiter, config=config)

ready = cached | fresh.axis_summaries
merged = {name: ready[name] for name in axes}      # порядок — из axes, см. 5.10

meta = await summarize_meta(client=client, model=model, axis_summaries=merged,
                            prompts=prompts, limiter=limiter, config=config)
```

---

## 4. `amnesiac` — корень пакета

Корневой `__init__` не реэкспортирует подсистемы (D-010). В корне доступно только то, что не тянет опциональных зависимостей.

### `Doc` — стабильно на `0.x`

```python
class Doc(BaseModel):
    model_config = ConfigDict(frozen=True, extra="forbid")

    text: str
    channel: str
    day_number: int
    doc_id: str | int | None = None
```

Единица входных данных суммаризации.

`text` — текст документа в том виде, в каком он пойдёт в промпт. Предобработка (нормализация, чистка разметки, отсев дайджестов) — на стороне потребителя, пакет текст не трогает.

`channel` — источник, попадает в строку промпта.

`day_number` — номер дня относительно начала окна. Величина производная, но она входит в рендер, поэтому является частью контракта. Считает её потребитель: он владеет `run_date` и горизонтом. Пакет использует её только для подстановки в шаблон, арифметики над ней не делает и ограничений на диапазон не накладывает. Ключом сортировки она служит в `select` (§6, `order_by`), а не в суммаризации: `summarize` порядок документов не меняет и получает их уже упорядоченными.

`doc_id` — идентификатор в терминах потребителя. Пакет его никогда не интерпретирует и не сравнивает, только протаскивает через отбор в результат для регресса и трассировки. Тип намеренно допускает и `int`, и `str`, чтобы потребитель не приводил `message_id` туда-обратно на границе.

Ограничение: `doc_id` нельзя использовать как ключ словаря на стороне потребителя без предварительного приведения к одному типу — `1` и `"1"` дадут разные ключи.

`extra="forbid"`: приложить произвольные метаданные к `Doc` нельзя. Если потребителю нужна сопутствующая информация, он держит её в собственной структуре, ключом по `doc_id`.

### Исключения — стабильно на `0.x`

В корне доступны `AmnesiacError` и `ConfigurationError`. Полная иерархия — раздел 8.

---

## 5. `amnesiac.summarize`

### 5.1 `summarize()` — стабильно на `0.x`

```python
async def summarize(
    *,
    client: AsyncOpenAI,
    model: str,
    axes: Mapping[str, Sequence[Doc]],
    prompts: PromptPack,
    limiter: AbstractAsyncContextManager | None = None,
    config: SummarizeConfig | None = None,
) -> SummarizeResult
```

Все аргументы keyword-only: это позволяет добавлять параметры и менять порядок, не ломая вызовы.

`client` — асинхронный клиент, совместимый с OpenAI SDK. Пакет обращается только к `client.chat.completions.create(...)`. Кто провайдер, откуда взят ключ, какой базовый URL и таймаут — не его дело.

`axes` — отображение «имя оси → документы». Порядок ключей значим: он определяет порядок блоков в мета-промпте. Ось с пустым списком документов допустима и обрабатывается как обычная — вызов к модели всё равно делается, ответ модели о нехватке контента отсекается на стадии меты правилом промпта. Пустое `axes` — `ConfigurationError`.

`prompts` — набор шаблонов, см. 5.4. Должен быть уже связан (`bind`) со всеми параметрами, которых нет в рантайме; недостающий ключ при рендере даёт `PromptRenderError`.

`limiter` — ограничитель параллелизма, любой асинхронный контекстный менеджер. Пакет делает только `async with limiter:` вокруг каждого обращения к модели. Если не передан — поднимается `asyncio.Semaphore(config.concurrency)`. Передача `limiter` вместе с явно заданным `config.concurrency` — `ConfigurationError`: это ошибка конфигурации, и мы падаем, а не выбираем молча (D-011).

`config` — значения, см. 5.3. `None` означает конфигурацию по умолчанию.

Возвращает `SummarizeResult`.

Поднимает `TooManyAxisFailures`, если упавших осей больше `config.max_failed_axes`; исключение несёт и упавшие оси, и посчитавшиеся (§8). Поднимает `MetaSummaryError`, если мета-вызов исчерпал попытки на пустом ответе; исключение несёт результат первой стадии целиком — саммари, состав отказов, `usage` (§8). Оба — подклассы `SummarizeError`, поэтому существующий `except SummarizeError` ловит их как раньше.

Провайдерские исключения после исчерпания ретраев проходят наружу как есть и частичного результата **не несут** (D-012, §9). Потребителю, которому важно не терять первую стадию и на этом пути, следует вызывать стадии раздельно (§5.8). `asyncio.CancelledError` пробрасывается всегда.

### 5.2 Поведение — стабильно на `0.x`

**Порядок работы.** Все оси считаются параллельно под ограничителем, затем результаты собираются в мета-вызов. Мета-вызов один и идёт после всех осей.

**Ретраи.** Каждое обращение к модели — и axis, и meta — оборачивается ретраем: до `max_attempts` попыток, паузы берутся из `retry_delays` по номеру попытки, последнее значение повторяется, если попыток больше, чем пауз. Ретраятся транзиентные отказы провайдера:

| Условие | Введено |
|---|---|
| `json.JSONDecodeError` | v0.1 |
| `httpx.ReadTimeout` | v0.1 |
| `httpx.RemoteProtocolError` | v0.1 |
| `openai.APIConnectionError` | v0.1 |
| `openai.APITimeoutError` | v0.1 |
| `openai.RateLimitError` | v0.2 (D-029) |
| `openai.InternalServerError` | v0.2 (D-029) |
| успешный ответ с пустым содержимым | v0.2 (D-028) |

Список — часть контракта и не настраивается: потребитель, которому нужен другой состав, повторяет вызов у себя. Прочие исключения уходят наружу с первой попытки. В частности, `openai.PermissionDeniedError` (403) **не повторяется**: наблюдался как временный отбойник на пути к провайдеру, но неотличим от просроченного ключа, и цена ошибки в двух прочтениях несимметрична (D-029).

**Пустой ответ модели.** Ответ без текста (`choices[0].message.content is None`) считается транзиентным отказом и повторяется наравне с сетевыми, по тем же `max_attempts` и `retry_delays`. Исчерпав попытки, он приравнивается к отказу: на оси — к отказу оси со всеми последствиями ниже, на мете — к `MetaSummaryError`. Отменяет D-017 (решение D-028): наблюдения второго потребителя показали до четверти пустых ответов на осевых вызовах при том, что повтор того же запроса проходит.

**Наблюдаемое следствие.** На неуспешных путях число обращений к провайдеру выросло — до `max_attempts` там, где в v0.1 была одна попытка. Видно через `usage.calls` и по длительности. На успешном пути ничего не изменилось: ни выхода, ни числа вызовов.

**Деградация при падении оси.** Ось, исчерпавшая ретраи или вернувшая пустой ответ, считается упавшей. Её имя попадает в `failed_axes`, текст ошибки — в `axis_errors`, а в `axis_summaries` вместо саммари подставляется `config.failed_axis_placeholder`. Заглушка **уходит в мета-промпт наравне с настоящими саммари** — это зафиксированное поведение, а не флаг. Если упавших осей больше `max_failed_axes`, мета-вызов не делается вовсе и поднимается `TooManyAxisFailures`.

Порог — абсолютное число упавших осей, не доля от общего количества. Считается он от осей, **запрошенных в этом вызове**: при частичном пересчёте четырёх осей из девяти `max_failed_axes=2` означает две из четырёх. Поведение не меняется, но потребитель, досчитывающий хвост, фактически работает под более жёстким порогом и при необходимости поднимает его сам.

**Отмена.** `asyncio.CancelledError` не глотается ни ретраем, ни логикой деградации. Общий таймаут задаёт вызывающий код: пакет не оборачивает работу в `wait_for`.

**Логирование.** Пакет пишет в `logging.getLogger("amnesiac.…")` и не настраивает хендлеры, уровни и формат. `print` не используется.

### 5.3 `SummarizeConfig` — стабильно на `0.x`

```python
class SummarizeConfig(BaseModel):
    model_config = ConfigDict(frozen=True, extra="forbid")

    temperature: float = 0.3
    concurrency: int = 5
    max_attempts: int = Field(3, ge=1)
    retry_delays: tuple[float, ...] = Field((5.0, 15.0, 45.0), min_length=1)
    max_failed_axes: int = 2
    failed_axis_placeholder: str = "(нет данных по оси из-за ошибки провайдера)"
```

`max_attempts` и `retry_delays` валидируются: ноль попыток и пустой кортеж пауз недостижимы. Без ограничений `retry_delays=()` даёт `IndexError` из внутренностей ретрая — сообщение об ошибке указывает не на причину (D-018).

Граница с аргументами `summarize()`: в конфиг попадают **значения** с осмысленным умолчанием, аргументами передаются **ресурсы сетапа** — клиент, модель, ограничитель, данные, промпты. `client` и `limiter` в конфиге отсутствуют принципиально.

`temperature` применяется к обеим стадиям одинаково. Раздельная температура для axis и meta в v0.1 не предусмотрена.

Умолчания воспроизводят поведение `blind_prophet` без изменений.

### 5.4 `PromptPack` — стабильно на `0.x`

```python
class PromptPack(BaseModel):
    model_config = ConfigDict(frozen=True, extra="forbid")

    name: str
    axis_system: str
    axis_user: str
    meta_system: str
    meta_user: str
    doc_template: str = "[день {day_number} | {channel}] {text}"
    doc_separator: str = "\n"
    axis_block_template: str = "=== Ось: {axis} ===\n\n{summary}"
    axis_block_separator: str = "\n\n"
    params: Mapping[str, Any] = {}

    def bind(self, **kwargs: Any) -> PromptPack: ...
```

Шаблоны рендерятся через `str.format`.

`doc_template`, `doc_separator`, `axis_block_template`, `axis_block_separator` вынесены в поля именно потому, что находятся под построчным регрессом: строка документа и разделитель блоков — такая же часть промпта, как и системное сообщение.

`bind(**kwargs)` возвращает **новый** пак с обновлённым `params`; исходный не меняется. Ключи, переданные в `bind`, доступны всем шаблонам.

Подстановки, которые пакет добавляет в рантайме:

| шаблон | рантайм-ключи |
|---|---|
| `axis_system` | `axis` |
| `axis_user` | `axis`, `docs` |
| `meta_system` | — |
| `meta_user` | `axis_blocks` |
| `doc_template` | `day_number`, `channel`, `text` |
| `axis_block_template` | `axis`, `summary` |

Всё остальное берётся из `params`. Отсутствие нужного ключа на момент рендера — `PromptRenderError` с указанием шаблона и имени ключа, а не голый `KeyError`.

**Паки иммутабельны по имени.** Правка текста существующего пака запрещена: новый текст — новое имя (`RU_MACRO_V2`). Иначе версия пакета перестаёт идентифицировать промпт, и построчный регресс теряет смысл.

### 5.5 `RU_MACRO_V1` — стабильно на `0.x`, текст заморожен

`amnesiac.summarize.prompts.RU_MACRO_V1` — пак, перенесённый из `blind_prophet` без изменений: русскоязычная суммаризация макроэкономического новостного потока с относительной датировкой.

Требует `bind(horizon_days=...)` перед использованием.

**Известные ограничения, унаследованные из исходного кода:**

- `meta_system` содержит литеральные «9 тематических таймлайнов» и «14-дневного периода» — это не плейсхолдеры. `bind(horizon_days=...)` на мета-промпт не влияет, и при другом горизонте или другом числе осей текст будет расходиться с фактическими данными молча. Исправление означает новый пак, а не правку этого;
- инструкции про относительную датировку прописаны под русский язык и российский макроконтекст. Для другого домена нужен свой пак.

### 5.6 `SummarizeResult` — стабильно на `0.x`

```python
class SummarizeResult(BaseModel):
    meta: str
    axis_summaries: dict[str, str]
    failed_axes: list[str]
    axis_errors: dict[str, str]
    usage: Usage
```

`meta` — итоговое мета-саммари.

`axis_summaries` — саммари по осям в порядке входного `axes`. Значения всегда `str`: пустой ответ модели по §5.2 становится отказом оси, поэтому `None` в словарь не попадает. Для упавших осей содержит `failed_axis_placeholder`, а не отсутствующий ключ: словарь всегда покрывает все запрошенные оси, и его содержимое совпадает с тем, что реально ушло в мета-промпт.

`failed_axes` — имена упавших осей. Пустой список при полном успехе.

`axis_errors` — «ось → `repr` последнего исключения». Живые объекты исключений в модели не хранятся: они не сериализуются и провоцируют удержание трейсбеков.

`usage` — агрегат по всем вызовам.

### 5.7 `Usage` — стабильно на `0.x`

```python
class Usage(BaseModel):
    prompt_tokens: int = 0
    completion_tokens: int = 0
    total_tokens: int = 0
    calls: int = 0

    def __add__(self, other: Usage) -> Usage: ...
```

Суммируется по всем обращениям к модели, включая неудачные попытки, если провайдер вернул `usage`. С ретраем пустого ответа (§5.2) неудачных попыток стало больше, и `calls` это отражает.

`__add__` складывает поля попарно и возвращает новый объект; операнды не меняются. Существует ради раздельных стадий: потребитель, вызвавший `summarize_axes()` и `summarize_meta()`, обязан свести их `usage` сам, а сложение четырёх полей вручную — операция, которую можно однажды сделать неверно и не заметить (D-031). Сложение с чем-либо, кроме `Usage`, даёт `TypeError`.

Стоимость не считается: в стандартной схеме OpenAI её нет, OpenRouter отдаёт её отдельным запросом к `/generation`, а такой запрос — сетевое обращение вне контракта пакета. Расчёт стоимости — на стороне потребителя, по токенам и собственному прайсу.

Разбивка usage по осям в v0.1 не предусмотрена.

### 5.8 `summarize_axes()` — стабильно на `0.x`

```python
async def summarize_axes(
    *,
    client: AsyncOpenAI,
    model: str,
    axes: Mapping[str, Sequence[Doc]],
    prompts: PromptPack,
    limiter: AbstractAsyncContextManager | None = None,
    config: SummarizeConfig | None = None,
) -> AxisSummariesResult
```

Первая стадия отдельным вызовом: считает осевые саммари и останавливается. Мета-вызова не делает.

Аргументы совпадают с `summarize()` по составу и смыслу, включая правило про `limiter` вместе с явным `config.concurrency` — `ConfigurationError` (D-011). Пустое `axes` — `ConfigurationError`.

Поднимает `TooManyAxisFailures` по тому же порогу. Прочие условия отказа — как в §5.2.

Существует ради двух сценариев: частичного пересчёта (§5.10) и случая, когда потребителю нужно сохранить дорогую первую стадию до того, как дешёвая вторая получит шанс упасть.

### 5.9 `AxisSummariesResult` — стабильно на `0.x`

```python
class AxisSummariesResult(BaseModel):
    axis_summaries: dict[str, str]
    failed_axes: list[str]
    axis_errors: dict[str, str]
    usage: Usage
```

Совпадает с `SummarizeResult` (§5.6) во всём, кроме отсутствующего `meta`. Правила те же: словарь покрывает все запрошенные оси, для упавших содержит `failed_axis_placeholder`, значения всегда `str`.

**Заглушка не является саммари.** Ось из `failed_axes` несёт в `axis_summaries` заглушку, и та по D-014 честно уходит в мету. Потребитель, кеширующий результат, обязан класть в кеш только оси, которых нет в `failed_axes`: иначе заглушка сохранится как результат, ось больше никогда не пересчитается, а дата останется с дыркой, неотличимой от успеха.

### 5.10 `summarize_meta()` — стабильно на `0.x`

```python
async def summarize_meta(
    *,
    client: AsyncOpenAI,
    model: str,
    axis_summaries: Mapping[str, str],
    prompts: PromptPack,
    limiter: AbstractAsyncContextManager | None = None,
    config: SummarizeConfig | None = None,
) -> MetaResult
```

Вторая стадия отдельным вызовом: собирает мету из готовых осевых саммари. Документов не видит и не запрашивает.

```python
class MetaResult(BaseModel):
    meta: str
    usage: Usage
```

**Порядок блоков определяется порядком ключей `axis_summaries`** — это часть контракта, а не свойство реализации. Потребитель, склеивающий кешированные оси с досчитанными, обязан восстановить исходный порядок явно: `{name: ready[name] for name in axes}`. Наивное объединение словарей даёт порядок «сначала кешированные, потом свежие», то есть другой промпт при тех же данных, и построчный регресс на стороне потребителя разъедется молча.

Откуда взялись саммари — пакету неизвестно и безразлично: они могли быть посчитаны этим же процессом, прочитаны из кеша потребителя или собраны из двух источников. Никакой проверки происхождения нет.

Пустое `axis_summaries` — `ConfigurationError`. Исчерпание попыток на пустом ответе — `MetaSummaryError`, у которого поле `axis_summaries` заполнено переданным входом, а `failed_axes` и `axis_errors` пусты: этот вызов о них ничего не знает.

`limiter` принимается для единообразия и оборачивает единственный вызов. Смысла ограничивать один запрос нет, но сигнатура держится симметричной, чтобы потребитель передавал один и тот же набор ресурсов в обе стадии.

### 5.11 Композиция — стабильно на `0.x`

`summarize()` эквивалентна последовательному вызову `summarize_axes()` и `summarize_meta()` при одних и тех же аргументах: тот же текст промптов, то же число обращений к провайдеру, тот же результат. Эквивалентность находится под тестом и является обещанием, а не совпадением реализации.

Одно отличие, и оно в пользу композиции: `summarize()` разрешает ограничитель один раз и передаёт его обеим стадиям внутренним путём. Раздельные вызовы, сделанные без явного `limiter`, поднимут по своему умолчательному семафору — на общее число одновременных запросов это не влияет, поскольку стадии идут последовательно, а мета-вызов один.

Потребителю, у которого весь набор осей считается с нуля, рекомендуется `summarize()`: он короче и не даёт забыть про порядок ключей. Раздельные стадии — для частичного пересчёта и для сохранения промежуточного результата.

---

## 6. `amnesiac.select`

### `select_by_axis()` — **unstable на весь `0.x`** (D-005)

```python
def select_by_axis(
    X: np.ndarray,
    queries: Mapping[str, np.ndarray],
    *,
    texts: Sequence[str],
    top_k: int,
    dedup_threshold: float,
    order_by: Sequence[float] | None = None,
) -> dict[str, list[int]]
```

Чистая синхронная функция. Сетевых вызовов не делает, `Doc` не знает, эмбеддинги не считает: векторы приходят предрассчитанными (D-006).

`X` — матрица `(n, d)` векторов документов. Нормировать заранее не нужно, функция работает с нормированной копией и вход не меняет.

`queries` — «ось → вектор запроса». Допускается как `(d,)`, так и `(m, d)`: несколько запросов на ось усредняются и перенормируются внутри. Усреднение оставлено в пакете сознательно — это часть математики отбора, находящейся под регрессом.

**Тип входа не приводится.** Функция считает в том, что дал потребитель: `float32` на входе — `float32` внутри, `float64` — `float64`. На близких похожестях порядок отбора от типа зависит. `blind_prophet` приводит выход эмбеддера к `float32` у себя, до вызова, — эта конверсия лежит на границе потребителя по D-006 и в пакет не перенесена (D-021). Потребитель, желающий воспроизвести отбор `blind_prophet` строка в строку, обязан передавать `float32` и в `X`, и в `queries`.

`top_k` — положительное целое. Ноль и отрицательные отвергаются `ValueError` (D-022): в оригинале такое значение недостижимо, а `np.argpartition(sims, -0)[-0:]` вернул бы весь массив вместо пустого.

`texts` — тексты документов, в том же порядке, что строки `X`. Нужны дедупу: из пары похожих документов остаётся более короткий. Без них воспроизвести отбор нельзя.

`order_by` — ключ финальной сортировки, длиной `n`. Если задан, отобранные индексы сортируются по нему **стабильной** сортировкой поверх порядка убывания похожести. Если `None` — возвращается порядок убывания похожести после дедупа. Параметр существует именно для воспроизводимости: сортировка на стороне потребителя дала бы тот же результат только при точном совпадении стабильности и исходного порядка, и это слишком тонкое условие, чтобы держать его вне пакета.

Возвращает «ось → список индексов строк `X`». Индексы, не `Doc`: сопоставление с собственными записями делает потребитель.

При `n == 0` возвращает по пустому списку на каждую ось.

**Почему unstable.** Форма аргументов — прямой слепок текущего пайплайна. При подключении второго потребителя вероятна замена на структуру, описывающую корпус целиком, вместо трёх параллельных последовательностей, которые обязаны совпадать по длине.

---

## 7. Что пакет гарантирует по детерминизму

При одних и тех же входах `select_by_axis` возвращает один и тот же результат: сортировка стабильная, дедуп детерминирован, случайности нет.

`summarize` детерминизма не гарантирует и гарантировать не может — недетерминизм у провайдера. Пакет гарантирует лишь побайтовую воспроизводимость **текста промпта** при одинаковых `axes`, `prompts` и порядке ключей.

Для раздельных стадий гарантия та же и опирается на то же условие: текст мета-промпта воспроизводим при одинаковых `axis_summaries` **и порядке их ключей** (§5.10). Порядок — вход, а не деталь реализации; восстановить его при склейке кеша обязан потребитель.

---

## 8. Иерархия исключений — стабильно на `0.x`

```
AmnesiacError                      # база, amnesiac
├── ConfigurationError             # amnesiac
├── PromptRenderError              # amnesiac.summarize
└── SummarizeError                 # amnesiac.summarize
    ├── TooManyAxisFailures        # .failures, .axis_summaries, .usage
    └── MetaSummaryError           # .axis_summaries, .failed_axes,
                                   #   .axis_errors, .usage
```

`ConfigurationError` — несогласованные аргументы: пустое `axes`, одновременно заданные `limiter` и `concurrency`, несовпадение длин `X`/`texts`/`order_by`.

`PromptRenderError` — при рендере не хватило ключа подстановки. Несёт имя шаблона и имя ключа.

`TooManyAxisFailures` — упавших осей больше `max_failed_axes`. В `failures` — «ось → `repr` исключения» по всем упавшим осям. Мета-вызов при этом не делался.

С v0.2 исключение несёт также `axis_summaries` — саммари **посчитавшихся** осей — и `usage` по уже сделанным вызовам. В v0.1 наружу уходил только состав отказов, и оплаченная работа терялась вместе с исключением. Ключей упавших осей в `axis_summaries` нет: заглушка здесь не подставляется, поскольку мета-промпт не собирается (D-032).

`MetaSummaryError` — мета-вызов исчерпал попытки на пустом ответе. Несёт результат первой стадии целиком: `axis_summaries` в том же виде, в каком они ушли бы в мету (то есть с заглушками по D-014), `failed_axes`, `axis_errors` и накопленный `usage`. Введено ради того, чтобы провал дешёвой второй стадии не обесценивал дорогую первую: потребитель кладёт саммари в кеш и повторяет одним вызовом `summarize_meta()` вместо десяти (D-030).

Подкласс `SummarizeError` выбран сознательно: существующий `except SummarizeError` продолжает ловить этот случай, и обновление пакета не требует правок у потребителя, которому частичный результат не нужен.

Поднятое из `summarize_meta()`, оно несёт переданный вход в `axis_summaries`, пустые `failed_axes` и `axis_errors` (§5.10).

**Провайдерские исключения не оборачиваются.** Ошибки `openai` и `httpx` после исчерпания ретраев проходят наружу в исходном виде. Обоснование: провайдером владеет потребитель, подмена типа лишает его возможности разобрать причину — отличить исчерпание квоты от неверного ключа или отказа модели.

`asyncio.CancelledError` пробрасывается всегда и ни при каких условиях не превращается в отказ оси.

---

## 9. Отступления от кода `blind_prophet`

В v0.2 их ровно восемь; всё прочее перенесено байт-в-байт. Пункты 1–6 приняты в v0.1, пункты 5, 7 и 8 введены или переформулированы в v0.2.

Не считаются отступлениями и в список не входят: конверсии типов на границе потребителя (D-021) и валидация аргументов, отсекающая недостижимые из `blind_prophet` состояния (D-018, D-022). Первое — код, оставшийся у потребителя по D-002/D-006; второе не меняет поведения ни на одном входе, который в оригинале мог возникнуть.

1. `SummarizeResult` возвращает axis-саммари и usage — сейчас они теряются внутри пайплайна.
2. `print` заменён на `logging`.
3. Ограничитель параллелизма инжектируется снаружи вместо `asyncio.Semaphore(5)`, создаваемого внутри. Умолчание сохраняет прежнее значение 5.
4. Мета-вызов обёрнут в тот же ретрай, что и axis-вызовы. В исходном коде он не защищён вообще. На успешном пути выход не меняется, поэтому регресс не затрагивается.
5. Пустой ответ модели обрабатывается явно и **повторяется** как транзиентный отказ (§5.2). Оригинал этот случай не проверяет и уводит `None` дальше по пайплайну. Формулировка v0.1 («приравнивается к отказу, повторная попытка не делается») заменена решением D-028. На успешном пути выход не меняется, поэтому регресс не затрагивается.
6. `asyncio.CancelledError`, возникшая на оси, пробрасывается наружу. Оригинал классифицирует отменённую ось как успешную — `isinstance(r, Exception)` ложно для наследника `BaseException`, — после чего объект исключения уходит в текст мета-промпта, а мета-вызов отправляется провайдеру. Порт как есть невозможен без нарушения §5.2 и §8. На успешном пути выход не меняется, поэтому регресс не затрагивается (D-020).
7. Список транзиентных отказов расширен на `RateLimitError` и `InternalServerError` (§5.2). Оригинал повторял только транспортные сбои, отдавая 429 и 5xx наружу с первой попытки. На успешном пути выход не меняется (D-029).
8. Исключения несут частичный результат: `TooManyAxisFailures` — посчитавшиеся оси, `MetaSummaryError` — результат первой стадии целиком (§8). В оригинале посчитанное теряется вместе с исключением. Наблюдаемо только на путях отказа (D-030, D-032).

Валидация `SummarizeConfig` (§5.3) отступлением не считается: она отсекает состояния, недостижимые из `blind_prophet`, и на переносимом поведении не сказывается.

Раздельные стадии (§5.8–5.10) и `Usage.__add__` (§5.7) в список не входят: это новые точки входа, а не иное поведение существующих. Поведение `summarize()` они не меняют — эквивалентность композиции зафиксирована §5.11 и находится под тестом.

---

## 10. Что **не** является публичным API

Опираться на перечисленное потребителю нельзя; это меняется без предупреждения и без записи в этом документе.

- Любое имя, начинающееся с `_`.
- Раскладка модулей внутри подпакетов: `amnesiac.summarize.summarizer`, имена внутренних функций (`_call_with_retry`, помощники стадий и подобные), их сигнатуры и факт существования. Публичны три точки входа: `summarize`, `summarize_axes`, `summarize_meta` — и только они. **Изменение против v0.1**, где непубличными были объявлены поимённо `summarize_axis` и `summarize_meta`: раздельные стадии выведены в контракт решением D-030, и запрет на эти имена снят вместе с тестом, который его сторожил.
- Точный текст промптов как объект правки. Читать `RU_MACRO_V1.axis_system` можно, рассчитывать на конкретные формулировки при разборе выхода модели — нет.
- Внутренности ретрая: число попыток по умолчанию, конкретные паузы, порядок логируемых сообщений.
- Формат строк логов и имена логгеров ниже `amnesiac`.
- Структура `axis_errors`, `TooManyAxisFailures.failures` и `MetaSummaryError.axis_errors` как парсимый формат: это `repr` для человека и логов, не машинный контракт. Опираться на `.axis_summaries` и `.usage` тех же исключений — можно, они часть контракта по §8.
- Число обращений к провайдеру на путях отказа: `usage.calls` отражает реальность, но конкретное значение зависит от `max_attempts` и состава транзиентных отказов, которые в §5.2 могут пополняться в пределах `0.x`.
- Всё в `amnesiac.select` — unstable по D-005.
- `amnesiac.neuter` — в v0.2 отсутствует, планируется отдельным эпиком к релизу `0.3.0`. Импорт этого подпакета в потребителе означает опору на несуществующий контракт.
- Транзитивные зависимости: наличие `httpx`, `openai`, `numpy` в окружении — следствие реализации, а не обещание.
